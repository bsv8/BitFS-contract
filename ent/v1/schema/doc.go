// Package schema 是 bitfs-contract 的数据库契约真源（v1）。
//
// 设计说明：
// 1) 本目录按 BitFS 当前真实库结构对齐；
// 2) 以表结构事实为准，不保留过时口径；
// 3) 业务仓库只能消费生成产物，不直接改表；
// 4) 全表统一使用 id 作为内部行键（ent 默认主键语义）；
// 5) 业务键只承担业务唯一性约束（UNIQUE），不再作为物理主键；
// 6) 禁止把业务键重新定义成主键，避免和 ent 默认实体主键语义冲突。
package schema
